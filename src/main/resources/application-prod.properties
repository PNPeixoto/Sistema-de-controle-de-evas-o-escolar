# Production must validate the existing schema instead of mutating it.
spring.jpa.hibernate.ddl-auto=validate
app.security.state-store=${APP_SECURITY_STATE_STORE:redis}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}
