package com.peixoto.usuario.infrastructure.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS está exclusivamente no SecurityConfig.java — NÃO adicionar aqui
}
